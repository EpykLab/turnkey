# Pulled at Argo sync time (kustomize remote resource). Pin versions intentionally when upgrading Tekton.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - https://storage.googleapis.com/tekton-releases/pipeline/previous/v0.68.0/release.yaml
