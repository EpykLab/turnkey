apiVersion: v1
kind: Service
metadata:
  name: hello-placeholder
  labels:
    app.kubernetes.io/name: hello-placeholder
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: hello-placeholder
  ports:
    - name: http
      port: 80
      targetPort: http
